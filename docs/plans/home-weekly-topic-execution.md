# 功能执行单：本周灵感专题页

## 目标

- 本功能要跑通的最小业务闭环：
  - 运营在后台维护“本周灵感”专题的基础信息、封面图和本期推荐菜谱。
  - 小程序首页首屏右上卡可以跳到“本周灵感”专题页。
  - 用户在专题页查看本期推荐菜谱和往期推荐，可先把多道菜加入待安排，再在专题页下方 sheet 里一次性放进同一天同一餐次。
- 对应 V1 范围：
  - 属于首页运营与灵感菜谱曝光编排，不新增社区、评论、打卡、关注、排行榜或专题收藏体系。

## 本轮范围

- 小程序端：
  - 新增“本周灵感”专题页。
  - 首页入口支持跳转专题页。
  - 专题页支持把菜加入待安排，并在同一个安排 sheet 里批量写入同一计划餐次。
- 后端 API：
  - 新增本周灵感专题公开读取接口。
  - 新增后台专题维护接口。
  - 首页快捷入口白名单补专题页路径，默认“本周灵感”入口改用专题页。
  - `POST /meal-plans` 升级为支持同一餐次提交多道菜，并把计划摘要改为 `menuItems[]`。
- 后台管理：
  - 新增“运营 / 本周灵感”配置页。
  - 支持维护专题基础信息、封面图和本期推荐菜谱顺序。
- 共享契约：
  - 新增专题页公开响应、后台响应和写入 DTO。

## 本轮不做

- 不做评论、打卡、精华区、话题主持人、参与人数和任何社区互动流。
- 不做“收藏专题”以及“我的收藏专题”入口。
- 不做专题级“跨多天 / 跨多餐次”批量排程，只处理同一次 sheet 选中的同一天同一餐次。
- 不做节庆专题页；节庆仍保持独立运营线。
- 不做专题推荐统计、专题点赞或专题分享回流。

## 业务流程与页面行为门禁

### 业务流程

- 用户目标：
  - 在首页点进“本周灵感”后，快速看到本期运营推荐的几道菜，先收下想安排的菜，再一次性安排到同一顿饭里。
- 触发条件：
  - 首页首屏右上卡配置为“本周灵感”专题入口。
  - 或用户在专题页里点击往期推荐卡片。
- 主成功路径：
  - 进入专题页 -> 查看专题头图、类别、描述、本期推荐菜谱 -> 未加入“我的菜谱”的菜先加入 -> 已加入的菜点击“加入计划”进入待安排 -> 点击底部入口打开安排 sheet -> 选择日期和餐次 -> 待安排中的菜一次性写入同一个计划餐次。
- 失败 / 阻断路径：
  - 当前没有任何已发布专题时，专题页展示空态。
  - 指定 `topicId` 不存在时，专题页展示失败态并可返回。
  - 专题存在但某道推荐菜谱已不可曝光时，该菜从专题列表中自动过滤，不阻断整个专题读取。
- 结束状态：
  - 用户停留在专题页继续浏览，且待安排中的菜已经写入同一天同一餐次。
- 确认人 / 确认记录：
  - 当前对话已确认：专题页参考社区编排感，但不做社区能力；“本周灵感”不是泛灵感列表，而是独立运营专题页。

### 页面行为

- 页面入口：
  - 首页首屏右上卡。
  - 专题页内部“往期推荐”横滑卡片。
- 展示内容及用途：
  - 头图区：模糊背景、封面、专题名、推荐类别、期次/更新时间、一句描述。
  - 本期推荐：展示专题当前配置的全部推荐菜谱，至少 `3` 道。
  - 往期推荐：横向滑动卡片。
- 用户操作：
  - 点击某道菜，进入菜谱详情。
  - 点击“先加入”，把灵感菜加入“我的菜谱”。
  - 点击“加入计划”，把这道菜写入专题页待安排。
  - 点击底部统一入口，打开安排 sheet 并一次安排待安排里的所有菜。
  - 点击往期卡片，切换到对应专题。
- 加载 / 空态 / 失败态：
  - 首屏 skeleton。
  - 无专题空态。
  - 指定专题不存在或读取失败时显示失败态。
- 成功后的页面变化：
  - 读取成功后显示专题详情。
  - 菜加入待安排后，卡片状态改成“待安排”，底部入口展示当前数量。
  - 提交安排后，当前待安排队列清空，计划数据写入同一天同一餐次。
- 本流程不需要的页面数据：
  - 评论、打卡、话题标签、作者信息、关注关系、专题收藏关系、专题互动计数。

### 门禁结论

- [x] 业务流程已确认
- [x] 页面行为已确认
- [x] 现有接口、表和页面仅作为候选实现，没有被当作需求证据

## 领域与商业化评估

- 数据归属：`PLATFORM`
- Free 基础：
  - 所有人可读专题页和往期推荐。
- 付费增量：
  - 本轮不涉及。
- 权益作用域：`不付费`
- 权益类型：`功能`
- 到期与超额行为：
  - 不涉及。
- 数据保留与迁出：
  - 历史专题保留为往期推荐。
- 配置来源：`GLOBAL`
- 隐私、安全与合规：
  - 只使用已可曝光的灵感菜谱，不暴露用户主页或社区关系。
- 是否涉及 Reserved 的 OCR、AI、Pro 或多家庭：
  - 否。

## 接口契约

| 方法 | 路径 | 用途 | 权限 | 幂等 | 版本字段 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| `GET` | `/home-topics/current` | 读取当前“本周灵感”专题页 | 公开 | 否 | 无 | 本轮新增 |
| `GET` | `/home-topics/{topicId}` | 读取一个指定专题页 | 公开 | 否 | 无 | 本轮新增 |
| `GET` | `/admin/home-topics` | 读取后台专题列表与当前编辑详情 | `AdminBearerAuth` | 否 | 无 | 本轮新增 |
| `POST` | `/admin/home-topics` | 新建一个专题 | `AdminBearerAuth` | 是 | 无 | 本轮新增 |
| `PUT` | `/admin/home-topics/{topicId}` | 更新一个专题 | `AdminBearerAuth` | 是 | `version` | 本轮新增 |
| `POST` | `/admin/home-topics/{topicId}/status` | 切换专题上架状态 | `AdminBearerAuth` | 是 | `expectedVersion` | 本轮新增 |
| `POST` | `/admin/home-topics/{topicId}/image` | 上传或替换专题封面图 | `AdminBearerAuth` | 是 | `expectedVersion` | 本轮新增 |
| `DELETE` | `/admin/home-topics/{topicId}/image` | 清空专题封面图 | `AdminBearerAuth` | 是 | `expectedVersion` | 本轮新增 |

## 最小数据表与约束

| 主事实 / 关系 | owner | 必要字段 | 生命周期 | 外键 / 唯一 / Check | 对应真实查询 | 是否复用现有结构 |
| --- | --- | --- | --- | --- | --- | --- |
| `HomeTopic` | `PLATFORM` | 标题、副标题、推荐类别、状态、期次、描述、封面图、版本、发布时间 | 新建、更新、上/下架、长期保留为往期 | `issueNo` 唯一；期次非负；版本正数 | 当前专题、按 `topicId` 读专题、按时间列往期 | 否 |
| `HomeTopicItem` | `PLATFORM` | `topicId`、`recipeId`、排序 | 新建、重排、删除 | `(topicId, recipeId)` 唯一；`topicId + sortOrder` 唯一；`recipeId` 外键到 `Recipe` | 读取专题内推荐菜谱顺序 | 否 |

- 可以从现有主事实重算、不新增持久化的内容：
  - 菜谱标题、封面、分类、点赞数、收藏数、难度、时长。
- 明确不新增的表、字段、枚举和索引：
  - 不新增专题收藏、专题点赞、专题评论和专题打卡表。
- migration 部署与回退边界：
  - 本地开发阶段按新 migration 直接前进，不保留旧占位契约 fallback。

## 风险与遗留

- 风险：
  - 计划数据已升级为“单餐次多菜子项”；后续若计划页要支持拖拽换天、换餐次或餐内排序，需要继续沿这套子项结构补真实排序合同。
  - 当前只支持 `LISTED / UNLISTED` 两态，不支持草稿态和定时发布。
- 遗留：
  - 节庆专题页独立执行。
  - 若后续需要专题标签、专题分享或专题排序策略，单独开执行单。

## 后续交互方向（已确认，当前未实现）

- 当前对话已确认的后续方向：
  - 周刊页已经收口为“先加入待安排，再一次性安排到同一个计划餐次”。
  - 计划页后续可以支持把餐次里的菜拖到不同日期/餐次，或在同一餐次内拖拽调整顺序。
- 当前明确不在本轮实现的内容：
  - 不新增计划页拖拽排序/跨餐次拖拽的后端合同。
  - 不把“计划页拖拽”提前拆进本轮周刊页改动。
- 后续真正进入计划页执行单时，再单独确认：
  - 拖拽排序是否只在本地即时生效，还是需要真实落库保存 `sortOrder`。
  - 跨日期 / 跨餐次拖拽时，目标餐次的菜单子项覆盖、合并和冲突规则。

## 范围自检

- 本次满足的用户确认规则：
  - 参考社区专题编排感，但不做社区能力。
  - 保留封面模糊背景、专题头图、本期推荐和往期横滑。
  - 不做收藏专题。
- 每个文件为什么必须修改：
  - 待实现后回填。
- 明确没有顺手加入的功能：
  - 不加评论、打卡、精华、关注、专题收藏。
- 因复用未被证明而没有提前增加的抽象：
  - 不抽通用“运营专题中心”，先只落本周灵感一条线。
- 是否还能缩小改动而不破坏需求：
  - 当前已收成一个专题页闭环，不再继续扩节庆专题或社区链路。
