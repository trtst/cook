# 当前实现差异清单

## 定位

现有代码是尚未上线的脚手架，直接按当前产品方案重构，不保留旧业务接口、旧状态模型或多饭搭子兼容逻辑。

## 已完成替换

1. 当前饭搭子统一使用 `/dining-groups/current`。
2. 删除客户端本地饭搭子偏好、切换页面和创建页面。
3. 接受邀请已原子完成原空间冻结、成员关系、当前空间和邀请消费。
4. seed 已保证每个测试用户都有单人空间与 `UserSpace`。
5. Prisma 已增加 `UserSpace`、迁出快照头、审计、Outbox 和当前幂等结构。
6. 退出已原子恢复原空间、结束成员关系并创建快照头。

## 仍待实现

1. 配置中心和 Plus 权益解析。
2. 原空间迁入任务与迁出快照清单项。
3. 空间账本、菜谱、图片和技术快照。
4. 冰箱、计划、购物、饭局和口味。

## 当前实施顺序

1. 建立配置、权益和空间状态解析。
2. 建立原空间迁入与迁出快照清单。
3. 建立菜谱、图片与技术快照。
4. 建立冰箱、计划、参与、购物、饭局与口味。

详细 Schema 与 SQL 方案见 `docs/plans/prisma-schema-plan.md`。
