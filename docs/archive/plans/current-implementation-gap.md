# 当前实现差异清单

> **已归档（2026-07-25）**：本文记录的是旧“唯一当前空间/迁出快照”阶段的实现差异，不再作为当前实现依据。后续开发请改读 `../../project.md`、`../../api-index.md`、`../../api-contract.md` 和 `../../plans/personal-data-refactor-plan.md`。

## 定位

现有代码是尚未上线的脚手架，直接按当前产品方案重构，不保留旧业务接口、旧状态模型或多饭搭子兼容逻辑。

## 已完成替换

1. 当前饭搭子统一使用 `/dining-groups/current`。
2. 删除客户端本地饭搭子偏好、切换页面和创建页面。
3. 接受邀请已原子完成原空间冻结、成员关系、当前空间和邀请消费。
4. seed 已保证每个测试用户都有单人空间与 `UserSpace`。
5. Prisma 已增加 `UserSpace`、迁出快照头、审计、Outbox 和当前幂等结构。
6. 退出已原子恢复原空间、结束成员关系并创建快照头。
7. 服务端配置目录、最小 Plus 授权、有效权益接口和 `2/6` 人席位解析已建立。
8. 本人当前可用迁出快照列表已实现，并按有效期即时过滤。

## 仍待实现

1. 原空间迁入任务与迁出快照清单项及读取契约。
2. 空间账本、菜谱、图片和技术快照。
3. 冰箱、计划、购物、饭局和口味。
4. 会员订单、升级和到期处理。

## 当前实施顺序

1. 冻结迁出快照清单项读取契约，再建立原空间迁入与迁出快照清单项。
2. 建立菜谱、图片与技术快照及真实空间状态。
3. 建立冰箱、计划、参与、购物、饭局与口味。
4. 在会员订单契约冻结后建立升级和到期处理。

当前空间在真实账本落地前继续返回真实 `0` 使用量和 `NORMAL`，不提前伪造空间占用。

详细 Schema 与 SQL 方案见 `prisma-schema-plan.md`。
